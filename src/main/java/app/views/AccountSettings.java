package app.views;

import com.vaadin.flow.component.Text;
import com.vaadin.flow.component.orderedlayout.VerticalLayout;

public class AccountSettings extends VerticalLayout {
    public AccountSettings() {
        add(new Text("AccountSettigs"));
    }
}
